language: node_js
node_js:
  - "6"
  - "7"
  - "8"
  - "9"
  - "10"

cache: yarn

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH

install:
  - yarn --no-lockfile

script:
  - yarn test
